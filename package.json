{
  "name": "internet-status-alarm",
  "version": "1.0.0",
  "description": "A really simple webpage that alerts whenever your internet is down. Helpful when you are downloading a large file and want to be notified in case something is wrong.",
  "main": "src/index.html",
  "scripts": {
    "start": "parcel src/index.html --out-dir docs --open",
    "build": "parcel build src/index.html --out-dir docs --public-url ./",
    "deploy": "npm run build && git add --all && git commit -m \"latest build\" && git push origin master",
    "push": "git add --all && git commit -m \"latest build\" && git push origin master"
  },
  "dependencies": {
    "axios": "0.20.0"
  },
  "devDependencies": {
    "@babel/core": "7.2.0",
    "parcel-bundler": "^1.6.1"
  },
  "keywords": []
}
